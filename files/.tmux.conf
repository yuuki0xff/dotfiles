
set-option -g prefix C-k
bin C-k send-prefix
unbind-key C-b

# vimのキーバインドを使う
setw -g mode-keys vi

# クリップボードにコピー、及びcopy-mode-viの直感的でない挙動を修正
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-pipe "xclip -i -selection clipboard"
bind -T copy-mode-vi y send -X cancel
unbind -T copy-mode-vi Enter
bind -T copy-mode-vi Enter send -X copy-pipe "xclip -i -selection clipboard"
bind -T copy-mode-vi Enter send -X cancel

bind -r h select-pane -L
bind -r j select-pane -D
bind -r k select-pane -U
bind -r l select-pane -R

bind -r C-j select-window -t :-
bind -r C-l select-window -t :+

bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

#再描画
bind -r C-r refresh-client

bind '"' split-window -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"

set-option -g default-terminal "screen-256color"

# vim: ft=conf
